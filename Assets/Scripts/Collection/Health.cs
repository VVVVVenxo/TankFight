using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Health : MonoBehaviour
{
/*     [SerializeField] private float healthAmount = 50f; // 加血量

    private void OnTriggerEnter(Collider other)
    {
        TankHealth tankHealth = other.GetComponent<TankHealth>(); // 获取碰撞对象的 TankHealth 组件
        if (tankHealth != null)
        {
            tankHealth.Heal(healthAmount); // 调用 TankHealth 中的 Heal 方法增加血量
            Destroy(gameObject); // 销毁加血物体
        }
    } */
}